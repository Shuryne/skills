interface:
  display_name: "Python Backend Conventions"
  short_description: "Apply an opinionated Python backend stack"
  default_prompt: "Use $python-backend-conventions to build this service with the repository's established Python backend conventions."
policy:
  allow_implicit_invocation: true
