interface:
  display_name: "Codebase Cleanup"
  short_description: "Audit code and docs before making changes"
  default_prompt: "Use $codebase-cleanup to audit this project and propose a prioritized cleanup plan before editing."
policy:
  allow_implicit_invocation: true
