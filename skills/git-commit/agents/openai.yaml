interface:
  display_name: "Git Commit"
  short_description: "Create atomic, convention-following commits"
  default_prompt: "Use $git-commit to create atomic commits that follow this repository's conventions."
policy:
  allow_implicit_invocation: false
