interface:
  display_name: "Git Commit"
  short_description: "Create safe, coherent Git commits"
  default_prompt: "Use $git-commit to inspect the current changes and create coherent commits that follow this repository's conventions."
policy:
  allow_implicit_invocation: false
